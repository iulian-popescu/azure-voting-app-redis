apiVersion: v1
kind: Service
metadata:
    name: iulianpopescuazurevotingappredis
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: iulianpopescuazurevotingappredis